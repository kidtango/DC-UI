import React from 'react';
import Typography from '@material-ui/core/Typography';

export const Risk: React.FC = () => {
  return (
    <div>
      <Typography variant='h1'>Risk</Typography>
    </div>
  );
};
