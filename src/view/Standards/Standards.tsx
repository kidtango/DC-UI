import React from 'react';
import { Typography } from '@material-ui/core';

export const Standards: React.FC = () => {
  return (
    <div>
      <Typography variant='h1'>Standards</Typography>
    </div>
  );
};
